$i=1; Get-ChildItem *.jpg | Sort-Object Name | ForEach-Object { Rename-Item $_ "$i.jpg"; $i++ }

Get-ChildItem *.jpg | ForEach-Object {
    $nome = [System.IO.Path]::GetFileNameWithoutExtension($_.Name)
    & "D:\Program Files\Tesseract-OCR\tesseract.exe" "$($_.Name)" "$nome" -l eng
}

* CONVERTER O TXT ABAIXO PARA UM ARQUIVO JSON ONDE CADA LINHA É UM ELEMENTO DE UM ARRAY NO JSON E O TEXTO IRÁ PREENCHER A CHAVE "en" do objeto:
(COLAR O CONTEÚDO DO TXT)

* Agora, acrescente uma chave "ptbr" com a traducao do "en"

* Quero que transforme esse json aplicando .split(" ") e substituindo todas as suas strings por um array de strings.

* ACRESCENTE AS CHAVES "x1": 0, "y1": 0, "x2": 0, "y2": 0,

* Acrescente uma chave "ptbrpp" com exatamente o mesmo conteúdo da chave "ptbr".